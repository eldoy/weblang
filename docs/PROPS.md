These are the props used in Weblang state to communicate with the runner:

1. state.break - cancels further execution of a node's children
2. state.test - used for @if @then @else to setup which branch to run
